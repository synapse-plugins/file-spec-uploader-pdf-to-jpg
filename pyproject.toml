[project]
name = "pdf-to-image-uploader"
version = "2.2.4"
description = "pdf-to-image-uploader plugin"
requires-python = ">=3.10"
dependencies = [
    "synapse-sdk",
    "PyMuPDF",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.8",
]
